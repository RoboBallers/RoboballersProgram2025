#include <Calibration.h>
#include <math.h>
#include <LineDetection.h>

Calibration::Calibration() {
    // LineDetection *lineDetection;

}


void Calibration::calibrate() {
    // int* sensorVals = lineDetection->getSensorValues();
    // for (int i = 0; i < 48; i++) {
    //     calibrateVal[i] = fmax(calibrateVal[i],sensorVals[i]+40);
    // }
}